name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: "npm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint

      # The suite carries the compliance tripwires (no tracker may appear that
      # would falsify the published cookie policy; no sponsoring entity or tax
      # ID may reappear in the page layer). Without this step they gate nothing.
      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm run build
